import './header.css';

import HomeButton from "./home-button/home-button";

function Header() {
  return (
    <header className="page-header">
      <HomeButton />
    </header>
  );
}

export default Header;